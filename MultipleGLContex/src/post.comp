#version 460 core
layout(local_size_x = 16, local_size_y = 16, local_size_z = 1) in;
layout(rgba8, binding = 0) uniform image2D return_image;

void main() {
    imageStore(return_image, ivec2(gl_GlobalInvocationID.xy), vec4(0,1,0,1));
}
